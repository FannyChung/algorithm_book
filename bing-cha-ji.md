# 并查集

适合情况：

* 快速查询两个元素是否同一个集合
* 快速合并两个元素所在的集合

合并策略：

* 经典：把size小的树放在size大的树下
* 展平：将路上遇到的所有节点都放到根节点下。

效率：

平均为O\(1\)



## 矩阵连线

01值的矩阵，值为1的连在一起视为岛，求矩阵中有多少个岛。

### 递归法

对每个为1的点向四周遍历，遍历为：递归遍历为1 的点，把这一片都比较为访问过（标记为2）。



